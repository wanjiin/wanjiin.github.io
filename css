html, body {
  margin: 0;
  padding: 0;
}

canvas {
  display: block;
}

#instructions {
  position: relative;
  font-family: "Lucida Console", "Courier New", monospace;
  text-align: center;
  color: blue;
}

#instructions:hover {
  font-family: Arial, Helvetica, sans-serif;
}
